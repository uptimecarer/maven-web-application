@Library ("ms-shared-library") _
pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                helloWorld()
            }
        }
        stage('Banner') {
            steps {
                baneerGreeting()
            }
        }
         stage('WelcomeName') {
            steps {
                wlcomeName("Raveendra")
            }
        }
    }
}
